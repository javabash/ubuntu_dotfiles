---
- defaults:
    link:
    relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.config: config

- create:
    - ~/Work

- shell:
  -
     command: ./setup_homebrew.zsh
     stdout: true
     stderr: true
...
