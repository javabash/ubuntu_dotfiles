---
- defaults:
    link:
    relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.config: config
    ~/.oh-my-zsh: oh-my-zsh
    ~/.zshenv: zshenv
    ~/.tmux.conf: tmux.conf
    ~/.ansiweatherrc: ansiweatherrc


- create:
    - ~/Work

- shell:
  -
     command: ./setup_homebrew.zsh
     stdout: true
     stderr: true
...
