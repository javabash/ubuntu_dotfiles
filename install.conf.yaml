---
- defaults:
    link:
    relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.config: config

- create:
    - ~/Work

- shell:
    - [git submodule update --init --recursive, Installing submodules]
...
